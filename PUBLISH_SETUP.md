# Настройка публикации на Gradle Plugin Portal

## 📋 Требования

1. Аккаунт на Gradle Plugin Portal (можно войти через GitHub)
2. API ключи для публикации
3. Настроенные GitHub Secrets

---

## 🔑 Шаг 1: Получение API ключей

### 1.1. Регистрация на Gradle Plugin Portal

1. Перейдите на https://plugins.gradle.org/
2. Нажмите **"Sign in"** в правом верхнем углу
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к вашему GitHub аккаунту

### 1.2. Создание API ключей

1. После входа перейдите в настройки: https://plugins.gradle.org/u/me
2. В разделе **"API Keys"** нажмите **"Create new key"**
3. Введите название ключа (например, "GitHub Actions")
4. Нажмите **"Create"**
5. **ВАЖНО**: Скопируйте и сохраните:
    - **Key** (публичный ключ)
    - **Secret** (приватный ключ)

   ⚠️ **Secret показывается только один раз! Сохраните его в безопасном месте.**

---

## 🔐 Шаг 2: Добавление секретов в GitHub

### 2.1. Откройте настройки репозитория

1. Перейдите в ваш репозиторий: https://github.com/haizerdev/impact-analysis-gradle-plugin
2. Нажмите **Settings** (вкладка настроек)
3. В боковом меню выберите **Secrets and variables** → **Actions**

### 2.2. Добавьте первый секрет (GRADLE_PUBLISH_KEY)

1. Нажмите **"New repository secret"**
2. Заполните:
    - **Name**: `GRADLE_PUBLISH_KEY`
    - **Secret**: вставьте ваш **Key** из Gradle Plugin Portal
3. Нажмите **"Add secret"**

### 2.3. Добавьте второй секрет (GRADLE_PUBLISH_SECRET)

1. Снова нажмите **"New repository secret"**
2. Заполните:
    - **Name**: `GRADLE_PUBLISH_SECRET`
    - **Secret**: вставьте ваш **Secret** из Gradle Plugin Portal
3. Нажмите **"Add secret"**

### 2.4. Проверьте секреты

В разделе **Actions secrets** должны быть видны два секрета:

- ✅ `GRADLE_PUBLISH_KEY`
- ✅ `GRADLE_PUBLISH_SECRET`

---

## 🚀 Шаг 3: Включение публикации в workflow

После добавления секретов нужно включить публикацию в GitHub Actions:

### 3.1. Отредактируйте `.github/workflows/publish.yml`

Найдите строку:

```yaml
if: false  # Temporarily disabled until secrets are configured
```

Измените на:

```yaml
if: true  # Secrets configured, publishing enabled
```

Или просто удалите строку `if: false`.

### 3.2. Закоммитьте изменения

```bash
git add .github/workflows/publish.yml
git commit -m "chore: enable Gradle Plugin Portal publishing"
git push origin main
```

---

## 📦 Шаг 4: Публикация новой версии

### 4.1. Обновите версию в `build.gradle.kts`

```kotlin
version = "1.0.3"  // Увеличьте версию
```

### 4.2. Создайте и запушьте тег

```bash
# Коммит с новой версией
git add build.gradle.kts
git commit -m "chore: bump version to 1.0.3"
git push origin main

# Создание и пуш тега (это запустит publish workflow)
git tag -a v1.0.3 -m "Release v1.0.3"
git push origin v1.0.3
```

### 4.3. Проверьте публикацию

1. GitHub Actions: https://github.com/haizerdev/impact-analysis-gradle-plugin/actions
2. Gradle Plugin Portal: https://plugins.gradle.org/plugin/com.nzr.impactanalysis

---

## 🧪 Локальная публикация (опционально)

Если хотите протестировать публикацию локально:

### Windows (PowerShell)

```powershell
# Установите переменные окружения
$env:GRADLE_PUBLISH_KEY="ваш-ключ"
$env:GRADLE_PUBLISH_SECRET="ваш-секрет"

# Опубликуйте плагин
./gradlew publishPlugins
```

### Linux/Mac

```bash
# Экспортируйте переменные
export GRADLE_PUBLISH_KEY="ваш-ключ"
export GRADLE_PUBLISH_SECRET="ваш-секрет"

# Опубликуйте плагин
./gradlew publishPlugins
```

---

## ✅ Проверка успешной публикации

После успешной публикации:

1. **GitHub Actions** покажет ✅ зеленую галочку
2. **GitHub Release** будет создан автоматически
3. **Gradle Plugin Portal** покажет новую версию (может занять 5-10 минут)

### Использование опубликованного плагина

```kotlin
plugins {
    id("com.nzr.impactanalysis") version "1.0.3"
}
```

---

## 🔧 Troubleshooting

### Ошибка: "Missing publishing keys"

**Причина**: Секреты не настроены в GitHub
**Решение**: Выполните Шаг 2

### Ошибка: "Plugin already exists"

**Причина**: Пытаетесь опубликовать версию, которая уже существует
**Решение**: Увеличьте версию в `build.gradle.kts`

### Ошибка: "Invalid credentials"

**Причина**: Неправильные API ключи
**Решение**:

1. Проверьте ключи на https://plugins.gradle.org/u/me
2. Пересоздайте секреты в GitHub

### Workflow не запускается

**Причина**: Нет тега или тег не в формате `v*`
**Решение**: Создайте тег в формате `v1.0.x`

---

## 📚 Полезные ссылки

- **Gradle Plugin Portal**: https://plugins.gradle.org/
- **Ваш плагин**: https://plugins.gradle.org/plugin/com.nzr.impactanalysis
- **API Keys**: https://plugins.gradle.org/u/me
- **Документация**: https://plugins.gradle.org/docs/submit
- **GitHub Actions**: https://github.com/haizerdev/impact-analysis-gradle-plugin/actions

---

## 📝 Чеклист для публикации

- [ ] Зарегистрирован на Gradle Plugin Portal
- [ ] Созданы API ключи
- [ ] Добавлены секреты в GitHub (`GRADLE_PUBLISH_KEY`, `GRADLE_PUBLISH_SECRET`)
- [ ] Включена публикация в `publish.yml` (убран `if: false`)
- [ ] Обновлена версия в `build.gradle.kts`
- [ ] Создан и запушен тег
- [ ] Проверен статус в GitHub Actions
- [ ] Плагин появился на Gradle Plugin Portal

---

**Готово! После настройки секретов публикация будет происходить автоматически при создании новых тегов.** 🎉
